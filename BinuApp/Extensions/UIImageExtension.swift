//
//  UIImageExtension.swift
//  BinuApp
//
//  Created by Ryan on 25/5/25.
//

import Foundation
import SwiftUI

extension UIImage {
//    /**
//        Limits a UIImage Object to the specified maximum dimension.
//     
//     - Parameter maxDimension: The maximum width or height (in points) for the resized image.
//     - Returns: A new UIImage instance resized to fit within `maxDimension` or `nil` if the resize operation fails.
//     */
//    func resized(to maxDimension: CGFloat) -> UIImage? {
//        let aspect = size.width / size.height
//        let newSize: CGSize
//        
//        if aspect > 1 {
//            newSize = CGSize(width: maxDimension, height: maxDimension / aspect)
//        } else {
//            newSize = CGSize(width: maxDimension * aspect, height: maxDimension)
//        }
//        
//        UIGraphicsBeginImageContextWithOptions(newSize, false, 0)
//        draw(in: CGRect(origin: .zero, size: newSize))
//        let resized = UIGraphicsGetImageFromCurrentImageContext()
//        UIGraphicsEndImageContext()
//        return resized
//    }
}
